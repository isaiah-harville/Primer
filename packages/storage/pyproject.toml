[project]
name = "primer-storage"
version = "0.1.0"
description = "Primer content-addressed source storage, shared by services"
requires-python = ">=3.13"
dependencies = [
    "anyio",
    "fsspec",
]

[build-system]
requires = ["uv_build>=0.12,<0.13"]
build-backend = "uv_build"
